# Redirect default Netlify subdomain to primary domain
https://hydro-tonight.netlify.com/* https://whatsonatthehydrotonight.com/:splat 301!

# Get Lambda requests through the service worker by proxying
/_/* /.netlify/:splat 200